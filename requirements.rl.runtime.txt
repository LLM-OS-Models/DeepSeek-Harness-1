# verl/vllm/deepspeed/ray의 runtime 의존성 — `uv pip install --no-deps`가
# 건너뛰는 것들. requirements.rl.txt 다음에 설치:
#   uv pip install -r requirements.rl.runtime.txt
#
# 이 목록은 vllm 0.25.0+cu129 / verl 0.8.0 / deepspeed 0.19.3 / ray 2.56.1의
# METADATA에서 torch/cuda 관련 패키지를 제외하고 모은 것이다.
# torch/torchvision/torchaudio는 pyproject.toml에서 cu128 wheel로 pin됨.
#
# 참고: flashinfer-python은 vllm 0.25가 requires하지만 torch 2.11용 wheel이
# 아직 flashinfer.ai에 없다. 대신 VLLM_USE_FLASHINFER_SAMPLER=0로 끄고
# native sampler로 폴백한다 (validate_rollout.py / launch_rl.sh가 기본값).

# ===== vLLM runtime deps (non-cuda) =====
aiohttp>=3.13.3
anthropic>=0.71.0
blake3
cachetools
cbor2
cloudpickle
compressed-tensors==0.17.0
depyf==0.20.0
diskcache==5.6.3
einops
fastapi[standard]<0.137.0,>=0.133.0
fastsafetensors>=0.3.2
filelock>=3.16.1
ijson
jsonschema>=4.23.0
lark==1.2.2
llguidance<1.8.0,>=1.7.0
lm-format-enforcer==0.11.3
mcp
mistral_common[image]>=1.11.5
model-hosting-container-standards<1.0.0,>=0.1.14
msgspec
ninja
numba==0.65.0
openai-harmony>=0.0.3
opencv-python-headless>=4.13.0
opentelemetry-api>=1.27.0
opentelemetry-exporter-otlp>=1.27.0
opentelemetry-sdk>=1.27.0
opentelemetry-semantic-conventions-ai>=0.4.1
outlines_core==0.2.14
partial-json-parser
prometheus_client>=0.18.0
prometheus-fastapi-instrumentator>=8.0.0
pybase64
pynvvideocodec==2.0.4
python-json-logger
pyyaml
pyzmq>=25.0.0
quack-kernels>=0.3.3
sentencepiece
setproctitle
six>=1.16.0
starlette>=1.0.1
tiktoken>=0.6.0
tilelang==0.1.11
tokenspeed-mla==0.1.2
torchcodec>=0.14
watchfiles
xgrammar<1.0.0,>=0.2.1

# H200 / Blackwall 전용 kernels (cu128 stack과 호환)
# apache-tvm-ffi는 tilelang 0.1.11과 충돌하는 0.1.13+를 피하기 위해 0.1.10으로 pin.
# (0.1.13+에서 TypeAttr __ffi_repr__ 중복 등록 에러 발생)
apache-tvm-ffi==0.1.10
humming-kernels[cu12]==0.1.10
nvidia-cudnn-frontend>=1.19.1
nvidia-cutlass-dsl==4.6.1
nvtx==0.2.15

# ===== Ray runtime deps =====
msgpack<2.0.0,>=1.0.0

# ===== verl runtime deps =====
codetiming
hydra-core
omegaconf==2.3.1
pybind11
pylatexenc
tensordict!=0.9.0,<=0.10.0,>=0.8.0
tensorboardX
torchdata
dill

# ===== DeepSpeed runtime deps =====
py-cpuinfo
joblib
hjson
nvitop==1.7.1

# ===== 공통 유틸 =====
pyarrow>=19.0.0
